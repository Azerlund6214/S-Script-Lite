Если нужно именно по кнопке, и без перезагрузки страницы, то тут нужно воспользоваться ajax

пример ссылки:

onclick="return xHttp('editfile.php?view=1&edit=7', 'result'); (подразумевается присутствие ajax скрипта, через который будет выполнен запрос)